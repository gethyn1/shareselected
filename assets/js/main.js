"use strict";
var App = App || {};

$(document).ready(function() {

	console.log('jQuery is loaded');

	// Initialise share highlighter
    var shareText = new App.Shareselected({
    	
    });
    shareText.init();
	
});